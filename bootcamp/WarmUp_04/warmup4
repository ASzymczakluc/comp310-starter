#include <stdio.h>
#include <string.h>

int main(){
  char String[] = "Hello";
  char Rev[] = "";
  int count = 0;
  for(i = 5, i > 0, --i){
    char loc[i];
    char Rev[count] = char loc[i];
    ++count; 
  }
printf(Rev);

}
