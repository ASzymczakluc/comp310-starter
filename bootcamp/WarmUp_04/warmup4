#include <stdio.h>
#include <string.h>

int main(){
  char String[] = "Hello";
  char Rev[] = "";
  int count = 0;
  int i;
  for(i = 5; i > 0; --i){
    char loc[i];
    Rev[count] = loc[i];
    ++count; 
  }
printf(Rev);

}
