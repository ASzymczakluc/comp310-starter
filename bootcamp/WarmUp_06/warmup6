#include <stdio.h>
#include <string.h>

int main(){
  int num = 5;
  printf("Factorial of ", num, " is: ",factorial(num));
  return 0;
}

int factorial(int n){
  if (n <= 0) {
            return 1;
        } else {
            return n * factorial(n - 1);
        }
    }
