#include <stdio.h>
#include <string.h>

int main(){
  int thing[] = {1,2,3,4,5};
  int sum = 0; 
//maybe should be i < 4
  for (i = 0, i < 5, i++){
    sum += thing[x];
    }
  average = sum / 5; 
  printf("Sum:" + sum); 
  printf("Average:" + average); 
  return 0;
}
