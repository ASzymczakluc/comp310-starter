#include <stdio.h>
#include <string.h>

int main(){
  int i;
  int thing[] = {1,2,3,4,5};
  int sum = 0; 
  int average;
//maybe should be i < 4
  for (i = 0; i < 5; i++){
    sum += thing[i];
    }
  average = sum / 5; 
  printf("Sum:", sum); 
  printf("Average:", average); 
  return 0;
}
